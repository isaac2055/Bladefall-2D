- Fixed: The Outskirts western-breach updraft at (72, 0) ignored its requiresWorld gate in public/index.html activeEnvironmentFields(); the spring at (82, 0) was also ungated. Both now honor keep-west-seal, preventing fresh spawns from being launched and trapped under the shaft ceiling.
- Fixed: Bram's escort in Black Woods never completed at all. nextStage()'s escort payoff cannot fire there because Black Woods exits through physical zone streaming, and nothing else marked him done, so the revisit line keyed on n.done was unreachable. His truth lesson now concludes at the authored root wall (the Wl(9600,...) obstacle carrying rootWall:1): he is marked done/wait, a traveler-helped quest event is recorded so the state survives a reload, and he pays in the region's own currency plus a healed body rather than the random legendary that the generic path hands out. The escort-plate mechanism was rejected for this because followerOnly plates need Companion Command, which is not earned until Emberdeep.
- Fixed: public/index.html loaded bladefall-harness.js unconditionally, but the module was listed in neither public/sw.js nor build-deploy.sh, so a deployed or offline build 404'd on it. It is now shipped and cached like every other module.
- Fixed: scripts/release-check.mjs validated the asset manifest in one direction only (for each listed asset, does index.html reference it?), so a script index.html loads but nothing ships was invisible. It now also checks index -> manifest.
- Fixed: scripts/release-check.mjs carried its own copy of the asset list, which had drifted from build-deploy.sh and left bladefall-advancement.js and the eighteen Level 1 SFX files unverified for byte parity. The list is now parsed from build-deploy.sh, so the two cannot diverge (73 assets checked before, 93 after).
- Fixed: LLM-HANDOFF/make-working-copy.sh excluded public/index.html.pre-multiplayer.bak from every working copy, but recollection-player.html fetches that file to run a sealed Recollection. Lean copies therefore had a permanently broken postgame Recollection player. The exclusion is removed and the handoff notes that reclassified it as a discardable backup are corrected.
- Fixed: six stage guards in tests/ asserted pre-Frostfell source shapes and failed once the full suite was actually run after that pass. tests/hollow-marksman-stage.test.mjs additionally sliced its stage up to a banner comment that Frostfell was later authored in front of, so the slice silently spanned two levels; it now stops at the next authored block.
- Fixed: the Gilded Instinct Gift is implemented and its description no longer says it is inactive. Standing still makes unclaimed coins, Vault Keys and sealed Recollections within 560 units glint. It only draws attention to objects already placed, and never claims, opens or moves anything, so no route can come to depend on wearing it.
- Validated: the White Court aqueduct's Frostfell half. The connector is shortcut/opensFrom:'frost-sorcerer'/initiallySealed, so it cannot be used to skip forward out of Frostfell; the in-world plaque says the far sluice is barred; and the (14920,650) arrival lands on the twentieth finale landing, Pl(14550,1000,650), confirmed by dropping the knight there in the running game with no Blood lost. The White Court half necessarily waits for that level's own pass.
- Fixed: three Gifts described effects they did not have. rime-step, cinder-oath and hushed-shape declared hooks but had no hasGift() call, so equipping them did nothing while their catalog text promised a rule change -- worse than Gilded Instinct, which at least disclosed that it was inert. Rime Step now arms on touching ground and lets the next dash drag a wake that slows ordinary foes for 1.6s; Cinder Oath kindles dream-fire on the first melee strike after a dash; Hushed Shape lets an ordinary foe lose the trail after the player breaks line of sight and holds still for 1.1s, never within 190 units so nothing stops dead beside the knight. tests/gifts.test.mjs now fails if any Gift declares hooks without a runtime hook. steadfast remains the deliberate no-op.
- Open: five Echo hooks are declared but never read. They are courier-loop's portal:projectile/return-damage:mul, far-thread's portal:projectile/precision:add, road-knot's traveler:helped/recovery:add, sky-kindling's environment:air/fire-prime:seconds, and white-hush's status:ice/freeze-duration:mul. Every Echo still delivers at least one working effect, so none is inert -- each of these is a secondary hook, and two belong to chapters that have not had their pass (white-hush to Frost Sorcerer). An earlier note in this file claimed sky-kindling and courier-loop were fully inert; that was wrong. Echo hooks reach gameplay by being named directly, matched by one of useTool()'s generic suffix filters, or bridged through p.mods, and tests/echoes.test.mjs locks the dead list so it can only be edited deliberately downward.
- Fixed: TAS saveState() threw "Unsupported TAS state value" on every level that rolled an elite enemy, which covered Broken Causeway, The Updrafts and Hollow Marksman. The elite roll did e.elite=Object.assign({key:k},ELITE_AFFIX[k]), copying the affix's effect FUNCTION onto the entity, and the harness serializer rejects functions outright rather than dropping them. Nothing ever read the function back off the entity - it is applied once at spawn and the renderer only wants elite.n and elite.c - so the entity now stores key/n/c only. This means the documented save/restore branching facility, which the handoff presents as general, was silently unusable on three of the eight authored stages. tests/harness-state.test.mjs guards both halves.
- Addressed in run 1 (7.97.0; human difficulty review still pending): the Muster recall outside Frostfell was not noticeable in play (owner review, 2026-09-13). The four return regions install only three to five posts of three shared archetypes (MUSTER_ROSTERS) and apply no health, damage or notice change to any existing enemy, while Frostfell's own strike adds eleven foes and a one-time x1.55 health boost to everything. In the Warden the first post stands at x=10,900 of 15,000, two thirds of the level from the mine arrival at (330,0), and first-visit deaths persist, so the return reads emptier than the outward trip. The event itself works (strike, save, reload, any region, verified in a real campaign session); the shortfall is content. tests/muster-recall.test.mjs asserts that no unit's health is multiplied, which encodes the rejected reading of "no HP stacking" and must be rewritten with the feature. Specification and plan: LLM-HANDOFF/12-RECALL-WORK-ORDER.md.

- Run 1 changes the recall to 14/12/13/15 regional actors, four unique roles, one-time global ordinary-enemy boosts and first re-garrisoning. Nine focused checks plus the full 506-test suite pass. Historical run 1 bot evidence failed Warden and the recalled Causeway opening; do not equate those bot failures with a proven player softlock, or treat the other bot passes as full campaign completion. See LLM-HANDOFF/12-RECALL-WORK-ORDER.md.

- Run 2 resolves the Warden replay mismatch and recalled Causeway failure. The bot now compares full simulation snapshots and restores a pristine runtime between bootstraps. New crystal, portal, pickup and flight verbs pass focused routes; whole later-stage planning still fails at the exact segments listed in LLM-HANDOFF/12-RECALL-WORK-ORDER.md (510/510 tests pass). Those search failures are not proof of gameplay softlocks.
