# Essential files and copy authority

## Critical warning

Do **not** use the current Git index as the copy manifest. It tracks only 11
files. The modern game depends on many untracked modules and assets. A working
copy must be made from the current filesystem.

## Tier A — required editable/runtime source

Copy all of these:

- `public/index.html` — main composition root, current custom level geometry,
  game loop, compatibility runtime, UI, encounters, and version.
- every `public/bladefall-*.js` module — all system authorities listed in
  `04-SYSTEMS-AND-ARCHITECTURE.md`.
- `public/littlejs.min.js` — bundled engine dependency.
- `public/peerjs.min.js` — bundled co-op dependency; retain even while solo is
  the current acceptance boundary.
- `public/serve.mjs` — local server.
- `public/sw.js` and `public/manifest.webmanifest` — offline/PWA runtime.
- `public/authoring.html`, `public/dialogue-editor.html`, and
  `public/recollection-player.html` — creator and postgame surfaces.
- `public/index.html.pre-multiplayer.bak` — the preserved pre-reimagining game.
  Despite the extension this is a runtime asset, not a backup:
  `recollection-player.html` fetches it to run a sealed Recollection, and it
  ships in `build-deploy.sh` and `sw.js`. Omitting it breaks the postgame.
- icons in `public/icon.svg`, `public/icon-*.png`, and
  `public/apple-touch-icon.png`.
- `package.json` and `package-lock.json` — test/validator dependencies and exact
  install graph.
- `tests/` — the full behavioral/structural safety net.
- `scripts/` — focused browser validators, audits, capture tools, and release
  check.
- `build-deploy.sh` and `deploy-assets/` — reproducible release mirror.
- `.agents/AGENTS.md` — project-specific “do not deploy without permission” rule.
- this `LLM-HANDOFF/` folder.

## Tier B — required for a fully runnable/audio-faithful copy

- all `public/audio/`, including attribution, music, and Level 1 SFX;
- `public/music.mp3`, the legacy/original final-boss cue.

The August runnable copy measured about 79 MB versus a roughly 386 MB development
folder. These are historical measurements, not current size limits. The runnable
mode remains the recommended copy because it retains audio.

## Tier C — compact design authority

Copy the prose and current charter files, not all historical media:

- top-level `docs/*.md`;
- `docs/charters/*/charter.md` and `docs/charters/*/README.md`;
- root `TESTING.md`, `KNOWN_BUGS.md`, and `docs/tas/README.md`;
- `docs/charters/opening-package-integration.json`;
- `docs/charters/opening-systems-integration.json`;
- small `receipt.json` files under current charter validation folders when
  preserving proof is useful.

Especially important existing documents:

- `docs/metroidvania-foundation.md` — N01–N16 foundation and reuse boundary;
- `docs/datura-reimagining-roadmap.md` — historical 88-run program and narrative
  foundation (status table is stale after L05);
- `docs/subsequent-level-run-plan.md` — current per-level production discipline;
- `docs/opening-arc-systems-matrix.md` — compact reward economy and Levels 1–5;
- `docs/level-charters.md` and `docs/level-authoring.md` — evidence/authoring
  contract;
- charters for Levels 1–8 — regional intent, with latest follow-ups in this handoff;
- `docs/frostfell-return-proposal.md` — implemented/future distinction and ancillary ideas;
- `scripts/validate-frostfell.mjs`, `scripts/frostfell-route.mjs`, and
  `scripts/frostfell-muster-checks.mjs` — isolated route, search, persistence,
  engine and normal-input mine/music validation;
- `tests/tas-runtime.test.mjs`, `tests/frostfell-runtime.test.mjs`,
  `tests/warden-runtime.test.mjs` — focused runtime contracts.

## Derived — do not copy into every working model

- `netlify-deploy/` — regenerated from `public/` by `./build-deploy.sh`;
- `node_modules/` — regenerated by `npm install`;
- `docs/**/screens`, screenshots, MP4 route recordings, contact sheets, and
  historical visual atlases — useful archival evidence but not required for
  ordinary coding handoff;
- `.DS_Store`, local `.claude/`, transient browser data, and editor caches.

## Git history

Since 2026-09-11 the repository tracks the whole authoritative tree (modules,
tests, scripts, compact docs, audio attribution and handoff records);
`.gitignore` excludes `node_modules/`, `netlify-deploy/`, `.netlify/` and docs
media. GitHub `main` was fast-forwarded to `707e473` (version `7.96.0`) on
2026-09-13, so a fresh clone is a complete working copy after `npm install`.

**Hazard — this working copy lives in iCloud-synced `~/Desktop`.** Under disk
pressure macOS evicts file contents to iCloud and leaves `dataless` placeholders;
any content read then blocks until iCloud returns the file, so `git status`,
`cat` and the test suite hang rather than fail. On 2026-09-11/12 `.git/index` was
evicted mid-session; on 2026-09-13 every loose object and most of `public/` were,
and iCloud stalled for about ninety minutes while the account's storage was full.
Before git work here, check:

```bash
find .git -type f -print0 | xargs -0 ls -lO | grep -c dataless
```

Anything above zero means wait for iCloud (Finder → folder → "Download Now"),
not retry. Stats and directory listings never block; only content reads do. The
durable fix is to keep the clone outside iCloud-synced folders. Do not run a
destructive cleanup while resolving this.

## Canonical versus generated

| Canonical | Generated/secondary |
| --- | --- |
| `public/` | `netlify-deploy/` |
| `tests/`, `scripts/` | validator receipts/screenshots |
| `package*.json` | `node_modules/` |
| handoff + charter prose | baseline videos/contact sheets |
| `build-deploy.sh` + `deploy-assets/` | deploy output |

## Essential-file sanity check

In a copied project:

```bash
npm install
npm test
npm run serve
./build-deploy.sh
npm run release:check
```

If a browser page loads without music, first verify `public/audio/` and the
service-worker asset list. If it loads an old layout, verify the `VERSION` and
`CACHE_NAME`, use a cache-busting query during testing, and reload the service
worker rather than assuming the source edit failed.
